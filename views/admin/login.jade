include ./../includes/base

if (message.length > 0)
  div.alert #{message}
  
.container
  form.login(action='/login', method='post')
    div Posta elektronikoa
    input(type='text' name='email' placeholder='user@gmail.com' required)
    div Pasahitza
    input(type='password' name='password' placeholder='pasahitza' required)
    div
      button.login(type='submit') Sartu
      | Edota
      a(href='/')  hasierara
      |  joan
    


include ./../includes/foot
